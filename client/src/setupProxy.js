const { createProxyMiddleware } = require('http-proxy-middleware'); // only for local run

module.exports = function(app) {
  app.use(
    '/api',
    createProxyMiddleware({
      target: 'http://localhost:5000',
      changeOrigin: true,
    })
  );
};